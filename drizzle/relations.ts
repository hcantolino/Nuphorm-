import { relations } from "drizzle-orm";
import { users, uploadedFiles, technicalFiles } from "./schema";

// Define relations if needed in the future
// Currently just importing to ensure schema is loaded
